# Atlas - JupyterHub Python Dependencies
# AI/ML Libraries
chonkie>=1.7.0,<2
# GHSA-95ww-475f-pr4f affects Ragas' multimodal URL/file helper. Atlas exposes
# only the four text collection metrics guarded by Backend's MetricName enum.
ragas==0.4.3
ollama>=0.6.2
langchain>=0.3.0
# Ragas 0.4.3 still imports langchain_community.chat_models.vertexai, which
# is absent in langchain-community 0.4.x. Keep this compatibility cap until
# Ragas moves that import to the newer provider package.
langchain-community>=0.3.0,<0.4
# langchain-openai is NOT pulled in by langchain/langchain-community —
# notebook 02_langchain_rag imports it directly (ModuleNotFoundError
# at cell 1 without this pin).
langchain-openai>=0.2.0
# openai was previously only transitive (via llama-index); notebook
# 01_litellm_basics imports it directly — pin it explicitly.
openai>=1.40.0
llama-index>=0.11.0
transformers>=4.40.0
sentence-transformers>=3.0.0

# Database Clients
weaviate-client>=4.22.0
neo4j>=5.20.0
psycopg2-binary>=2.9.9
redis>=5.0.0
supabase>=2.7.0
sqlalchemy>=2.0.30

# HTTP and API
httpx>=0.27.0
requests>=2.33.0

# Configuration and Utils
python-dotenv>=1.0.1
pyyaml>=6.0.1

# Data Science (already in base image, but ensuring versions)
pandas>=2.2.0
numpy>=1.26.0
Pillow>=12.3.0

# Financial research kit - read-only market data and paper portfolio notebooks.
# Keep live-trading/exchange-key workflows out of this image slice; see
# atlas_finance.research guards and notebook 11.
openbb==4.7.2
openbb-yfinance==1.6.3
ccxt==4.5.68

# Lakehouse clients
# Keep boto3/s3fs aligned through botocore + fsspec to avoid slow resolver
# backtracking and to stay compatible with the notebook ML dependency set.
boto3==1.35.36
botocore==1.35.36
aiobotocore==2.15.2
s3fs==2024.9.0
fsspec==2024.9.0
pyiceberg[s3fs]==0.11.1
pyarrow==23.0.1
duckdb==1.5.5

# Visualization
matplotlib>=3.9.0
seaborn>=0.13.0
plotly>=5.22.0
networkx>=3.3

# Jupyter Extensions
ipywidgets>=8.1.0
jupyter-server-proxy>=4.1.0

# Deep Learning — PyTorch core
torch==2.11.0
torchvision==0.26.0
torchaudio==2.11.0

# Deep Learning — higher-level frameworks
torchmetrics==1.4.2
mlflow==3.14.0
label-studio-sdk==2.0.0

# Graph Neural Networks (PyG) — wheels matched to the torch 2.11 CPU build.
# pyg_lib (#776): the torch-2.11.0+cpu index carries ONLY 0.6.0, with cp311
# wheels for x86_64/macOS/Windows but NO linux-aarch64 wheel — and pyg_lib has
# no sdist and is not on PyPI, so an unsatisfiable pin hard-fails the image
# build (aborting the whole --cold bring-up). Platform-mark it: the optional
# GNN accelerator installs where a wheel exists and skips cleanly elsewhere
# (torch_geometric falls back to its own implementations). scatter/sparse/
# cluster DO ship linux-aarch64 wheels, so they stay unconditional.
--find-links https://data.pyg.org/whl/torch-2.11.0+cpu.html
torch-scatter==2.1.2
torch-sparse==0.6.18
torch-cluster==1.6.3
pyg_lib==0.6.0; platform_machine == "x86_64"
torch_geometric==2.6.1

# ML utilities
tqdm>=4.66.0
kaleido>=0.2.1

# Distributed compute
ray[client]>=2.56.0,<2.57
# Lightweight Spark Connect client (~1.5 MB, no JVM/Spark distro) — notebook
# 09_spark_connect talks to the spark-connect sidecar via sc://. Pinned to the
# server's Spark version (apache/spark:4.1.2); a mismatch breaks the gRPC handshake.
pyspark-client==4.1.2

# ml-lab runtime support — see https://github.com/thekaveh/ml-lab
# The package was previously published as `nnx-pytorch`; that distribution
# is deprecated. `thekaveh-nnx` is the renamed successor. The [lm] extra
# pulls language-model deps used by the ml-lab notebooks that `import nnx`.
thekaveh-nnx[lm]==0.2.1
python-louvain  # `community` module — community_detection-karate notebook
nltk>=3.10.0  # SentimentIntensityAnalyzer — sentiment_classification-vader notebook (vader_lexicon downloaded in Dockerfile)
spacy  # tokenizer + lemmatizer — text_classification-agnews + sentiment_classification-vader (en_core_web_sm downloaded in Dockerfile)
torchao>=0.17  # transitive via nnx.quantize_int8 + nnx.qat_train_step_factory — quantization-mnist notebook
prettytable  # nnx Utils.print_table renderer — used by 14 of 29 notebooks
fastmcp==3.4.5  # MCP client toolkit — matches services/mcp-servers runtime (#598, #600)
