# Docs IA Restructure (Duplication Kill) — Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax.

**Goal:** Eliminate the reader-facing documentation duplication — shrink the 659-line root `README.md` from a parallel mini-site to a thin landing, and replace the hand-maintained copies of ports/SOURCE facts with links to their generated canonical reference pages — without touching nav-section numbering.

**Architecture:** Reader-facing prose is de-duplicated by (a) moving the one README section with no canonical home (the project-structure tree) into a canonical page, then rewriting the README to link to canonical pages instead of restating them; (b) replacing the two big hand-maintained fact tables under `docs/deployment/` with a short intro + link to their generated `docs/reference/*` equivalents; (c) adding lightweight index pages to the internal archival trees. No `docs/manifest.yaml` section is reordered or renumbered.

**Tech Stack:** Markdown; the Atlas docs pipeline (`make docs-build` / `make docs-check`), `scripts/check-docs-drift.py`, `scripts/check_doc_links.py`, `bootstrapper/tools/validate_fragments.py`.

**This is PR #2 of 4** in `docs/superpowers/specs/2026-07-26-docs-overhaul-and-reskin-design.md`. Independently shippable.

## 1. Global Constraints

- **Generated vs hand-authored:** NEVER hand-edit a generated page. Generated (edit the generator/manifest instead): `docs/tracks.md`, `docs/services.md`, `docs/reference/*.md`, `docs/architecture/*`, and the README `<!-- TOPOLOGY:BEGIN/END -->` block (generated by `bootstrapper/tools/generate_readme_topology.py`). Hand-authored / freely editable: root `README.md`, `docs/index.md`, `docs/configuration.md`, `docs/core-concepts.md`, `docs/operations.md`, `docs/development.md`, `docs/README.md`, everything in `docs/deployment/` and `docs/quick-start/`, and the internal trees.
- **NO nav renumbering:** do not reorder or renumber `docs/manifest.yaml` sections, and do not change any `# N.` H1 on a manifest-sourced page. This PR must not touch manifest section order. (Nav reorder / folder rename / CHANGELOG split are DEFERRED to a follow-up — see Task 6.)
- **Keep the TOPOLOGY block:** the `<!-- TOPOLOGY:BEGIN -->…<!-- TOPOLOGY:END -->` table in `README.md` is generated + drift-checked by `validate_fragments`. Keep both markers and the table between them byte-for-byte; only edit prose OUTSIDE the markers.
- **README heading numbering:** the root README uses `## 1.`, `## 2.`, … H2 sections normalized by `scripts/number-markdown-headings.py`. After cutting/merging sections, the remaining H2s must be contiguously renumbered — run `uv run --project bootstrapper python scripts/number-markdown-headings.py` and verify `scripts/check-docs-drift.py` `numbered_headings` passes.
- **Gates that must stay green:** `scripts/check-docs-drift.py` (all probes incl. `content_quality`, `numbered_headings`, `professional_symbols`), `scripts/check_doc_links.py` (no broken links — every link you add must resolve), `bootstrapper/tools/validate_fragments.py` (README TOPOLOGY + fragments), `make docs-check`, and the full bootstrapper suite.
- **PNG-drift trap:** `make docs-build` rewrites `docs/diagrams/img/*.png`; `git restore docs/diagrams/img/` before committing. Targeted `git add` only; NEVER `git add -A`.
- **No information loss:** before cutting a README section, confirm its facts live in the canonical page you link to; if a fact is unique (only the project-structure tree), MOVE it to a canonical page first.
- ENVIRONMENT NOISE: every shell command prints a harmless first line about `/Users/kaveh/.zshenv` / `vmx-cargo` — ignore it.

---

### 1.1. Task 1: Relocate the README project-structure tree to `docs/development.md`

The only substantial README content with no canonical home is the project-structure tree (`README.md:523-585`, under §7.1). Move it into the hand-authored `docs/development.md` (nav §9.1) so §7 of the README can later be reduced to a link.

**Files:**
- Modify: `docs/development.md` (append a "Repository layout" section with the tree)
- Modify: `README.md` (the tree will be removed in Task 2 — here only ADD to development.md; do not yet cut the README)

**Interfaces:** none. Verified by `make docs-check` + `check_doc_links` green and the tree present in `docs/development.md`.

- [ ] **Step 1: Read the current tree + development.md**

Run: `sed -n '520,590p' README.md` and `cat docs/development.md`. Confirm `docs/development.md` is hand-authored (not generated) and note its existing section numbering.

- [ ] **Step 2: Add a "Repository layout" section to development.md**

Append a new `## N. Repository layout` section (next contiguous number) to `docs/development.md` containing the project-structure tree currently in `README.md:523-585` (a fenced ``` block — fenced content is exempt from the content-quality/heading gates). Keep it factual; do not add narration. Ensure any prose you add carries no marketing adjective or diagram-narration phrase.

- [ ] **Step 3: Renumber + verify**

Run: `uv run --project bootstrapper python scripts/number-markdown-headings.py` (normalizes development.md's H2s).
Run: `make docs-check` → exit 0; `uv run --project bootstrapper python scripts/check-docs-drift.py` → all PASS; `uv run --project bootstrapper python scripts/check_doc_links.py` → exit 0. `git restore docs/diagrams/img/`.

- [ ] **Step 4: Commit**

```bash
git add docs/development.md
git commit -m "docs: move project-structure tree into development.md (canonical home)"
```

---

### 1.2. Task 2: Shrink the root README to a thin landing

Rewrite `README.md` from a 659-line parallel mini-site to a ~150–200-line landing: hero, a tight quick-start, the (kept) TOPOLOGY block, a short overview, and a documentation link-map. Every section whose content is duplicated elsewhere becomes a one-to-three-line summary + link to its canonical page.

**Files:**
- Modify: `README.md`

**Interfaces:** none. Verified by the gates below.

- [ ] **Step 1: Establish the link targets (all must exist — verify with `ls`)**

Canonical homes to link to (confirmed generated/canonical): quick-start → `docs/quick-start/index.md` + `docs/quick-start/interactive-setup-wizard.md`; SOURCE model → `docs/reference/source-values.md` + `docs/core-concepts.md`; ports/routes → `docs/reference/ports-routes.md`; tracks → `docs/tracks.md`; services → `docs/services.md`; architecture → `docs/architecture/index.md`; troubleshooting → `docs/TROUBLESHOOTING.md`; reuse → `docs/deployment/reusing-atlas.md`; full doc map → `docs/README.md`; project layout → `docs/development.md` (from Task 1). `ls` each.

- [ ] **Step 2: Rewrite README to the thin structure**

Keep: the `# Atlas` hero (title, tagline, poster, top architecture SVG, wizard screenshot); a single `## Quick start` with the 30-second command block (`./start.sh`) + a one-line pointer to the wizard/quick-start pages; the `<!-- TOPOLOGY:BEGIN -->…END -->` block VERBATIM (do not touch between the markers) under a `## Service topology` heading; a short `## What is Atlas` (2–3 sentence factual overview — no marketing adjectives); a `## Documentation` link-map (bulleted links to the canonical pages from Step 1, mirroring `docs/README.md`); and the `## Contributing` / `## License` / `## Support` boilerplate.
Remove: §1.1 endpoint/port list (→ link to ports-routes), §1.2/§5.1 CLI combos (→ source-configuration), §1.3/§8 troubleshooting (→ TROUBLESHOOTING), §1.4 wizard prose (→ wizard page), §1.5/§2-tracklist (→ tracks), §3.4 SOURCE matrix (→ source-values), §4 service tables (→ services), §5.2/§6 access+GPU+reuse prose (→ deployment pages), §7.1 project tree (moved in Task 1), §9 hand-curated doc index (→ collapse into the new `## Documentation` map). Target ≤ 200 lines.
Every retained sentence must be factual and grounded; no diagram-narration captions (the two old image captions were already fixed in PR #1 — keep them terse); no marketing adjectives.

- [ ] **Step 3: Renumber, rebuild, verify ALL gates**

Run: `uv run --project bootstrapper python scripts/number-markdown-headings.py`.
Run: `make docs-build` (regenerates the TOPOLOGY block + confirms nothing else drifted); `git restore docs/diagrams/img/`.
Run each and confirm exit 0 / PASS: `make docs-check`; `uv run --project bootstrapper python scripts/check-docs-drift.py` (numbered_headings, professional_symbols, content_quality all PASS); `uv run --project bootstrapper python scripts/check_doc_links.py`; `uv run --project bootstrapper python -m tools.validate_fragments` (TOPOLOGY block intact).
Confirm `wc -l README.md` is ≤ ~200 and the TOPOLOGY markers + table are unchanged (`git diff README.md` shows no edits between the markers).

- [ ] **Step 4: Commit**

```bash
git add README.md
git commit -m "docs: shrink root README to a thin landing; link canonical pages instead of duplicating"
```

---

### 1.3. Task 3: De-link the hand-maintained SOURCE matrix

`docs/deployment/source-configuration.md` hand-maintains the full "Service SOURCE Support Matrix" (lines ~29-120) that duplicates the GENERATED `docs/reference/source-values.md`. Replace the hand table with a short intro + link.

**Files:**
- Modify: `docs/deployment/source-configuration.md`

**Interfaces:** verified by gates + the file no longer containing the full per-service matrix.

- [ ] **Step 1: Confirm the generated canonical**

`ls docs/reference/source-values.md` (generated by `reference_pages`). Read it to confirm it carries the authoritative SOURCE values, and read `docs/deployment/source-configuration.md` to identify the hand-maintained matrix block.

- [ ] **Step 2: Replace the matrix with intro + link**

In `docs/deployment/source-configuration.md`, replace the hand-maintained per-service SOURCE matrix table (and the localhost table if it merely restates the reference) with: a 1–2 sentence explanation of what a SOURCE value means + a link to `docs/reference/source-values.md` (the canonical list) and `docs/reference/env-vars.md`. KEEP the genuinely deployment-specific prose (CLI examples, GPU notes, migration guidance) that is NOT in the reference page. Do not remove a fact that has no canonical home — link, don't lose.

- [ ] **Step 3: Renumber + verify**

`uv run --project bootstrapper python scripts/number-markdown-headings.py`; `make docs-check` exit 0; `check-docs-drift.py` all PASS; `check_doc_links.py` exit 0; `git restore docs/diagrams/img/`.

- [ ] **Step 4: Commit**

```bash
git add docs/deployment/source-configuration.md
git commit -m "docs: de-duplicate SOURCE matrix in source-configuration.md (link to generated reference)"
```

---

### 1.4. Task 4: De-link the hand-maintained ports/routes copy

`docs/deployment/ports-and-routes.md` (86 lines) hand-maintains a ports/alias table duplicating the GENERATED `docs/reference/ports-routes.md`, and its intro even declares itself + the README as "authoritative sources" (`ports-and-routes.md:11`) — the drift hotspot.

**Files:**
- Modify: `docs/deployment/ports-and-routes.md`

- [ ] **Step 1: Confirm the generated canonical**

`ls docs/reference/ports-routes.md`; read it; read `docs/deployment/ports-and-routes.md` and locate the duplicated alias/port table (lines ~22-65) and the "authoritative sources" claim (~5-12).

- [ ] **Step 2: Replace the table + fix the self-designation**

Replace the hand-maintained alias/port table with a short intro + link to `docs/reference/ports-routes.md` (the single authoritative, generated list) and the README `## Service topology` block. Remove the sentence that names this file + the README as "authoritative sources"; the ONE authoritative source is the generated reference page. KEEP the genuinely deployment-specific content (per-engine quirks, localhost overrides) that isn't in the reference page.

- [ ] **Step 3: Renumber + verify**

`number-markdown-headings.py`; `make docs-check` exit 0; `check-docs-drift.py` PASS; `check_doc_links.py` exit 0; `git restore docs/diagrams/img/`.

- [ ] **Step 4: Commit**

```bash
git add docs/deployment/ports-and-routes.md
git commit -m "docs: de-duplicate ports/routes in deployment doc (single authoritative reference)"
```

---

### 1.5. Task 5: Add index pages to the internal archival trees

The internal trees (`docs/research/`, `docs/strategy/`, `docs/maintenance/`) stay out of the site nav but need a reader path. `docs/research/README.md` already exists; add matching lightweight indexes to `strategy/` and `maintenance/`, and add an "Internal / project docs" pointer to the root README's `## Documentation` map.

**Files:**
- Create: `docs/strategy/README.md`, `docs/maintenance/README.md`
- Modify: `README.md` (add a one-line "Project & internal docs" pointer under `## Documentation`)

**Interfaces:** these live under `_INTERNAL_DIRS` (already exempt from `check_docs` completeness/self-containment and from the `content_quality` gate) — but `check-docs-drift.py` numbered_headings + professional_symbols + `check_doc_links` DO scan them, so they must use strict `## 1.`-style numbering, no decorative symbols, and valid links.

- [ ] **Step 1: Write the two index pages**

`docs/strategy/README.md`: `# Strategy & evaluation notes` + a `## 1. Index` with a bulleted list linking each `docs/strategy/*.md` evaluation (one line each, factual). `docs/maintenance/README.md`: `# Maintenance notes` + `## 1. Index` linking `external-contract-ledger.md`. No decorative symbols; conform to the numbering scheme.

- [ ] **Step 2: Link from the README doc-map**

Add one bullet under the README `## Documentation`: "Project & internal docs: research, strategy, and maintenance notes live under `docs/` (see each folder's README)." Use a relative link that resolves (e.g. `docs/strategy/README.md`).

- [ ] **Step 3: Verify**

`uv run --project bootstrapper python scripts/number-markdown-headings.py`; `check-docs-drift.py` PASS; `check_doc_links.py` exit 0 (the new READMEs' links resolve); `make docs-check` exit 0; `git restore docs/diagrams/img/`.

- [ ] **Step 4: Commit**

```bash
git add docs/strategy/README.md docs/maintenance/README.md README.md
git commit -m "docs: add index pages for internal strategy/maintenance trees + README pointer"
```

---

### 1.6. Task 6: Full-suite verification + document the deferred nav work

**Files:** none (verification + a short note).

- [ ] **Step 1: Run the complete gate set**

From repo root, all must be green:
- `make docs-check` → exit 0
- `uv run --project bootstrapper python scripts/check-docs-drift.py` → all PASS
- `uv run --project bootstrapper python scripts/check_doc_links.py` → exit 0
- `uv run --project bootstrapper python -m tools.validate_fragments` → pass
- `PYTHONPATH=bootstrapper python -m bootstrapper.docs.regen --all --check` → exit 0
- `cd bootstrapper && uv run pytest -q` → full suite green
- `git restore docs/diagrams/img/` from repo root afterward.

- [ ] **Step 2: Record the deferred scope in the spec's open items**

Append to `docs/superpowers/specs/2026-07-26-docs-overhaul-and-reskin-design.md` §11 a note that PR #2 delivered the duplication-kill only, and the following are DEFERRED (they require a manifest-renumber ripple — reorder + H1 rebake on every hand-authored page + regen + H2 renumber, with `_validate_numbering` failing on drift): nav-section reordering, the `docs/deployment/` folder rename, collapsing the double generated track table (`docs/tracks.md` vs `docs/reference/tracks.md`), and splitting `CHANGELOG.md`/`ROADMAP.md`. This is a documented deferral, not a silent cap.

- [ ] **Step 3: Commit**

```bash
git add docs/superpowers/specs/2026-07-26-docs-overhaul-and-reskin-design.md
git commit -m "docs(spec): record PR #2 deferred nav-renumber scope"
```

## 2. Self-Review

**Spec coverage (§6.1):** README shrink → Tasks 1-2. One-home-per-fact (SOURCE, ports) → Tasks 3-4. Archival index → Task 5. Folder-coherence / thin-generated-stub merge / CHANGELOG split → explicitly DEFERRED with rationale (Task 6) because they force a manifest-renumber ripple; documented, not dropped.

**Placeholder scan:** none — each task names exact files, the cut-vs-link decision per README section (grounded in the current-state map), and machine verification via the gate set.

**Numbering-ripple safety:** no task reorders/renumbers manifest sections or edits a manifest-sourced H1; README/development.md H2s are renumbered by the repo's own `number-markdown-headings.py` and verified by `check-docs-drift`. Generated pages are never hand-edited.

**Link safety:** every task ends with `check_doc_links.py` green, so no cut-and-link introduces a 404 (the failure mode that bit PR #1).
