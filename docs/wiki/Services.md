# Services

## 1. Service Index

Generated wiki service index.

- airflow — Apache Airflow (DAG orchestrator) (agents, container)
- backend — Backend API (FastAPI) (apps, container)
- backup — Backup / restore (Postgres + volumes -> S3) (infra, container)
- blender-mcp — Blender MCP (media, virtual)
- celery — Celery + Flower (async jobs) (agents, container)
- chatterbox — Chatterbox (voice-cloning TTS, GPU) (media, container)
- cloud-providers — Cloud LLM providers (OpenAI, Anthropic, OpenRouter) (llm, virtual)
- cloudflared — Cloudflare Tunnel (public edge) (infra, container)
- comfyui — ComfyUI (image generation) (media, container)
- crawl4ai — Crawl4AI (JS-capable web extraction) (media, container)
- doc-processor — doc-processor (aggregate, doc-only)
- docling — Docling (document processor) (media, container)
- globals — Globals (project + branding) (infra, virtual)
- grafana — Grafana (observability UI + alerting) (infra, container)
- hermes — Hermes (programmable AI agent) (agents, container)
- iceberg-rest — Apache Iceberg REST Catalog (data, container)
- jenkins — Jenkins (Maven Spark app builder) (apps, container)
- jupyterhub — JupyterHub (DS/ML + LLM notebooks) (apps, container)
- kong — Kong (API gateway) (infra, container)
- label-studio — Label Studio (dataset review + annotation) (apps, container)
- langfuse — Langfuse (LLM traces + evals) (infra, container)
- lightrag — LightRAG (graph-augmented RAG server) (agents, container)
- litellm — LiteLLM gateway (LLM router) (llm, container)
- llm-graph-builder — Neo4j LLM Graph Builder (apps, container)
- local-deep-researcher — Local Deep Researcher (LangGraph research agent) (apps, container)
- loki — Loki (queryable log store) (infra, container)
- mcp-servers — Curated MCP Servers (agents, container)
- minio — MinIO (S3-compatible object storage) (data, container)
- mlflow — MLflow (experiment tracking + artifacts) (apps, container)
- multi2vec-clip — multi2vec-clip (aggregate, doc-only)
- n8n — n8n (workflow automation) (agents, container)
- neo4j — Neo4j (graph database) (data, container)
- ollama — Ollama (local LLM engine) (llm, container)
- open-webui — Open WebUI (chat interface) (apps, container)
- openclaw — OpenClaw (AI agent gateway) (agents, container)
- otel-collector — OpenTelemetry Collector (telemetry ingest) (infra, container)
- parakeet — Parakeet (NVIDIA STT engine) (media, container)
- prometheus — Prometheus (metrics scraper + TSDB) (infra, container)
- ray — Ray (distributed compute substrate) (infra, container)
- redis — Redis (cache & queue) (data, container)
- redpanda — Redpanda (Kafka API streaming) (data, container)
- searxng — SearXNG (privacy metasearch) (media, container)
- spark — Apache Spark (standalone cluster) (data, container)
- speaches — Speaches (unified TTS + STT) (media, container)
- stt-provider — stt-provider (aggregate, doc-only)
- supabase — Supabase (db, auth, api, storage, realtime, studio, meta) (data, container)
- supavisor — Supavisor (Postgres transaction pooler) (data, container)
- tei-reranker — TEI Reranker (mxbai-rerank-base-v1) (llm, container)
- tempo — Tempo (distributed trace store) (infra, container)
- tika — Apache Tika (fallback extractor) (media, container)
- trino — Trino (data, container)
- tts-provider — TTS provider (text-to-speech engine selector) (media, virtual)
- verba — Verba (archived Weaviate RAG UI) (apps, container)
- weaviate — Weaviate (vector database) (data, container)
- zeppelin — Apache Zeppelin (Spark-first notebook) (apps, container)
