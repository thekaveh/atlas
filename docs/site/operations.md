# Operations

Common commands:

```bash
./start.sh
./start.sh --setup-hosts
./stop.sh
./stop.sh --cold
./stop.sh --clean-hosts
```

Operational references include startup warnings, release notes, backup/restore,
and CI gates.
